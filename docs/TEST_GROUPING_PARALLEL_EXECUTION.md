# テストシナリオ並列実行グループ化計画
# Test Scenario Parallel Execution Grouping Plan

**作成日**: 2026-01-15
**バージョン**: 1.0

---

## 📋 目次

1. [概要](#概要)
2. [グループ化戦略](#グループ化戦略)
3. [グループ詳細定義](#グループ詳細定義)
4. [依存関係グラフ](#依存関係グラフ)
5. [Playwright設定](#playwright設定)
6. [実行手順](#実行手順)

---

## 概要

### 目的

87ページのテストシナリオを並列実行可能なグループに分類し、テスト実行時間を短縮する。

### グループ化原則

| 原則 | 説明 |
|------|------|
| **独立したテスト** | 状態を共有せず、同時に実行可能 |
| **同じページ別機能** | 同じページでも異なる機能テストは並列実行可能 |
| **状態を共有するテスト** | 認証状態、データ作成などが依存する場合は順次実行 |
| **リソース競合回避** | データベース書き込み、同一ユーザーセッション使用を分離 |

### 期待効果

| 指標 | 改善前 | 改善後 | 効果 |
|------|--------|--------|------|
| **実行時間** | ~120分 | ~30分 | **75%短縮** |
| **並列度** | 1 | 4グループ同時実行 | **4倍** |
| **リソース利用率** | 25% | 90% | **3.6倍** |

---

## グループ化戦略

### 並列実行可能グループ（6グループ）

```
┌─────────────────────────────────────────────────────────────┐
│                     テスト実行階層構造                       │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐        │
│  │   GROUP A   │  │   GROUP E   │  │   GROUP F   │        │
│  │  (完全並列)  │  │  (完全並列)  │  │  (完全並列)  │        │
│  │   公開ページ  │  │  リダイレクト │  │  DB接続確認  │        │
│  │    37テスト  │  │    9テスト   │  │    5テスト   │        │
│  └─────────────┘  └─────────────┘  └─────────────┘        │
│         ↓                ↓                ↓                │
│  ┌─────────────────────────────────────────────────────┐  │
│  │              同時実行可能（状態独立）                  │  │
│  └─────────────────────────────────────────────────────┘  │
│                                                             │
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐        │
│  │   GROUP B   │  │   GROUP C   │  │   GROUP D   │        │
│  │  (一部並列)  │  │   (順次実行)  │  │   (順次実行)  │        │
│  │  認証ページ   │  │  会員ポータル │  │  管理者ポータル│       │
│  │   12テスト   │  │   26テスト   │  │   18テスト   │        │
│  └─────────────┘  └─────────────┘  └─────────────┘        │
│         ↓                ↓                ↓                │
│  ┌─────────────────────────────────────────────────────┐  │
│  │          認証状態を共有 - グループ内順次実行           │  │
│  └─────────────────────────────────────────────────────┘  │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

---

## グループ詳細定義

### GROUP A: 公開ページ（完全並列）

**特徴**: 認証不要、状態独立、リソース競合なし

**サブグループ**（各サブグループ内で並列実行可能）:

| サブグループ | テスト数 | 対象ページ | 並列戦略 |
|-------------|----------|-----------|----------|
| **A-1: ホーム** | 3 | `/`, `/about`, `/news` | 完全並列 |
| **A-2: カタログ** | 5 | `/catalog`, `/catalog/[slug]` | 完全並列 |
| **A-3: 見積ツール** | 4 | `/quote-simulator`, `/smart-quote`, `/roi-calculator` | 完全並列 |
| **A-4: コンタクト** | 3 | `/contact`, `/inquiry/detailed` | 完全並列 |
| **A-5: その他** | 22 | `/samples`, `/guide/*`, `/premium-content`, etc. | 完全並列 |

**合計**: 37テスト

**並列実行コード**:
```typescript
// tests/e2e/group-a-public/01-home.spec.ts
import { test } from '@playwright/test';

test.describe('GROUP A-1: ホームページ（並列）', () => {
  test('トップページ読み込み', async ({ page }) => {
    await page.goto('/');
    // 独立したテスト - 同時実行可能
  });

  test('会社概要ページ', async ({ page }) => {
    await page.goto('/about');
    // 独立したテスト - 同時実行可能
  });

  test('ニュースページ', async ({ page }) => {
    await page.goto('/news');
    // 独立したテスト - 同時実行可能
  });
});
```

**実行方法**:
```bash
# 完全並列実行（4つ同時）
npx playwright test tests/e2e/group-a-public/ --workers=4
```

---

### GROUP B: 認証ページ（一部並列）

**特徴**: 一部テストで認証後状態を共有

**サブグループ**:

| サブグループ | テスト数 | 対象ページ | 並列戦略 |
|-------------|----------|-----------|----------|
| **B-1: ログイン（認証前）** | 4 | `/auth/signin`, `/auth/register` | 完全並列 |
| **B-2: パスワードリセット** | 2 | `/auth/forgot-password` | 完全並列 |
| **B-3: 認証後ページ** | 6 | `/auth/signout`, `/auth/pending`, `/auth/error` | 認証後に順次 |

**合計**: 12テスト

**並列実行コード**:
```typescript
// tests/e2e/group-b-auth/01-signin.spec.ts
import { test } from '@playwright/test';

test.describe('GROUP B-1: ログインページ（認証前並列）', () => {
  test('ログインフォーム表示', async ({ page }) => {
    await page.goto('/auth/signin');
    // 認証前 - 並列実行可能
  });

  test('バリデーションエラー', async ({ page }) => {
    await page.goto('/auth/signin');
    await page.fill('input[type="email"]', 'invalid-email');
    // 独立したテスト
  });
});

// tests/e2e/group-b-auth/02-after-auth.spec.ts
import { test } from '@playwright/test';

test.describe('GROUP B-3: 認証後（順次実行）', () => {
  // 認証状態を共有 - 順次実行必要
  test.beforeAll(async ({ browser }) => {
    const page = await browser.newPage();
    await page.goto('/auth/signin');
    await page.evaluate(() => {
      localStorage.setItem('dev-mock-user-id', 'test-member-001');
    });
    await page.close();
  });

  test('ログアウト機能', async ({ page }) => {
    await page.goto('/auth/signin');
    // 認証済み状態を使用
  });

  test('セッション管理', async ({ page }) => {
    await page.goto('/member/dashboard');
    // 同じセッションを継続
  });
});
```

---

### GROUP C: 会員ポータル（順次実行）

**特徴**: すべてのテストで同じ認証セッションを使用

**サブグループ**:

| サブグループ | テスト数 | 対象ページ | 並列戦略 |
|-------------|----------|-----------|----------|
| **C-1: ダッシュボード** | 3 | `/member/dashboard` | 順次実行 |
| **C-2: 注文管理** | 8 | `/member/orders/*` | 順次実行 |
| **C-3: 見積管理** | 5 | `/member/quotations/*` | 順次実行 |
| **C-4: プロフィール** | 4 | `/member/profile`, `/member/edit`, `/member/settings` | 順次実行 |
| **C-5: その他** | 6 | `/member/samples`, `/member/contracts`, etc. | 順次実行 |

**合計**: 26テスト

**順次実行コード**:
```typescript
// tests/e2e/group-c-member/member-portal.spec.ts
import { test } from '@playwright/test';

// グループ全体で1つの認証セッションを共有
test.describe.serial('GROUP C: 会員ポータル（順次実行）', () => {
  let authenticatedPage;

  test.beforeAll(async ({ browser }) => {
    const page = await browser.newPage();
    await page.goto('/auth/signin');
    await page.evaluate(() => {
      localStorage.setItem('dev-mock-user-id', 'test-member-001');
    });
    authenticatedPage = page;
  });

  test('C-1: ダッシュボード', async () => {
    await authenticatedPage.goto('/member/dashboard');
    // 認証済みセッション使用
  });

  test('C-2: 注文一覧', async () => {
    await authenticatedPage.goto('/member/orders');
    // 同じセッション継続
  });

  test('C-3: 注文詳細', async () => {
    await authenticatedPage.goto('/member/orders/test-order-001');
    // 順次実行
  });
});
```

**重要**: `test.describe.serial()` でテストを順次実行

---

### GROUP D: 管理者ポータル（順次実行）

**特徴**: 管理者認証セッションを共有、データ変更操作あり

**サブグループ**:

| サブグループ | テスト数 | 対象ページ | 並列戦略 |
|-------------|----------|-----------|----------|
| **D-1: 管理者ダッシュボード** | 3 | `/admin/dashboard` | 順次実行 |
| **D-2: 注文管理** | 4 | `/admin/orders/*` | 順次実行 |
| **D-3: 見積管理** | 3 | `/admin/quotations/*` | 順次実行 |
| **D-4: 生産管理** | 3 | `/admin/production/*` | 順次実行 |
| **D-5: その他** | 5 | `/admin/inventory`, `/admin/approvals`, etc. | 順次実行 |

**合計**: 18テスト

**順次実行コード**:
```typescript
// tests/e2e/group-d-admin/admin-portal.spec.ts
import { test } from '@playwright/test';

test.describe.serial('GROUP D: 管理者ポータル（順次実行）', () => {
  let adminPage;

  test.beforeAll(async ({ browser }) => {
    const page = await browser.newPage();
    await page.goto('/auth/signin');
    await page.evaluate(() => {
      localStorage.setItem('dev-mock-user-id', 'test-admin-001');
    });
    adminPage = page;
  });

  test('D-1: ダッシュボード統計', async () => {
    await adminPage.goto('/admin/dashboard');
    // 管理者セッション使用
  });

  test('D-2: 注文承認', async () => {
    await adminPage.goto('/admin/orders');
    // データ変更操作 - 順次実行必要
  });
});
```

---

### GROUP E: リダイレクト検証（完全並列）

**特徴**: 読み取り専用、状態変更なし

**サブグループ**:

| サブグループ | テスト数 | 対象リダイレクト | 並列戦略 |
|-------------|----------|----------------|----------|
| **E-1: Portal → Admin** | 4 | `/portal/*` → `/admin/customers/*` | 完全並列 |
| **E-2: B2B → Auth/Member** | 3 | `/b2b/*` → `/auth/*`, `/member/*` | 完全並列 |
| **E-3: その他** | 2 | `/roi-calculator` → `/quote-simulator` | 完全並列 |

**合計**: 9テスト

**並列実行コード**:
```typescript
// tests/e2e/group-e-redirects/redirects.spec.ts
import { test } from '@playwright/test';

test.describe('GROUP E: 301リダイレクト（完全並列）', () => {
  test('E-1-1: /portal → /admin/customers', async ({ page }) => {
    const response = await page.goto('/portal');
    expect(response?.status()).toBe(301);
    // 読み取り専用 - 並列実行可能
  });

  test('E-1-2: /portal/orders → /admin/customers/orders', async ({ page }) => {
    const response = await page.goto('/portal/orders');
    expect(response?.status()).toBe(301);
    // 独立したテスト
  });

  test('E-2-1: /b2b/login → /auth/signin', async ({ page }) => {
    const response = await page.goto('/b2b/login');
    expect(response?.status()).toBe(301);
    // 完全に独立
  });
});
```

---

### GROUP F: データベース接続確認（完全並列）

**特徴**: 接続確認のみ、データ変更なし

**サブグループ**:

| サブグループ | テスト数 | 対象 | 並列戦略 |
|-------------|----------|------|----------|
| **F-1: 正常接続** | 3 | APIエンドポイント接続 | 完全並列 |
| **F-2: 異常接続** | 2 | 認証エラー、権限エラー | 完全並列 |

**合計**: 5テスト

**並列実行コード**:
```typescript
// tests/e2e/group-f-database/connectivity.spec.ts
import { test } from '@playwright/test';

test.describe('GROUP F: データベース接続（完全並列）', () => {
  test('F-1-1: ダッシュボードAPI接続', async ({ page }) => {
    await page.goto('/auth/signin');
    await page.evaluate(() => {
      localStorage.setItem('dev-mock-user-id', 'test-member-001');
    });

    const apiResponses = [];
    page.on('response', response => {
      if (response.url().includes('/api/member/dashboard')) {
        apiResponses.push(response.status());
      }
    });

    await page.goto('/member/dashboard');
    expect(apiResponses[0]).toBeLessThan(500);
  });

  test('F-1-2: 注文API接続', async ({ page }) => {
    // 独立した接続テスト
  });

  test('F-2-1: 認証エラー確認', async ({ page }) => {
    await page.evaluate(() => localStorage.clear());
    await page.goto('/member/dashboard');
    await page.waitForURL(/\/auth\/signin/);
    // 独立したテスト
  });
});
```

---

## 依存関係グラフ

### グループ間依存関係

```
┌─────────────────────────────────────────────────────────────┐
│                   グループ間依存関係                          │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│   ┌─────────┐      ┌─────────┐      ┌─────────┐           │
│   │ GROUP A │      │ GROUP E │      │ GROUP F │           │
│   │ 公開ページ │      │ リダイレクト │    │ DB接続確認  │           │
│   │  (独立)  │      │  (独立)  │      │  (独立)  │           │
│   └────┬────┘      └────┬────┘      └────┬────┘           │
│        │                │                │                 │
│        └────────────┬───┴────────────────┘                 │
│                     ↓                                      │
│              並列実行可能（独立）                           │
│                     ↓                                      │
│        ┌──────────────────────────────┐                   │
│        │                              │                   │
│   ┌────┴────┐                    ┌────┴────┐              │
│   │ GROUP B │                    │ GROUP C │              │
│   │ 認証ページ │────── 認証 ─────→│ 会員ポータル│          │
│   └─────────┘                    └─────────┘              │
│        │                              │                   │
│        │                              ↓                   │
│        │                         ┌─────────┐              │
│        └──────── 管理者認証 ────→│ GROUP D │              │
│                                  │管理者ポータル│            │
│                                  └─────────┘              │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

### 実行順序のルール

| 順序 | 実行対象 | 理由 |
|------|----------|------|
| **1. 最初の波** | GROUP A + E + F（並列） | 状態独立、同時実行可能 |
| **2. 第二の波** | GROUP B（一部並列） | 認証処理 |
| **3. 第三の波** | GROUP C（順次） | 会員認証後 |
| **4. 最後の波** | GROUP D（順次） | 管理者認証後 |

---

## Playwright設定

### 完全並列実行設定

**ファイル**: `playwright.config.ts`

```typescript
import { defineConfig, devices } from '@playwright/test';

export default defineConfig({
  testDir: './tests/e2e',

  // 完全並列実行を有効化
  fullyParallel: true,

  // ワーカー数（並列実行数）
  workers: 4,  // 4つのテストを同時実行

  // テストの失敗時の再試行
  retries: process.env.CI ? 2 : 0,

  // テスト失敗時に only を禁止
  forbidOnly: !!process.env.CI,

  // レポーター
  reporter: [
    ['html', { outputFolder: 'playwright-report' }],
    ['json', { outputFile: 'test-results.json' }],
    ['junit', { outputFile: 'junit-results.xml' }]
  ],

  // タイムアウト設定
  timeout: 60 * 1000,  // 各テスト60秒
  expect: {
    timeout: 10 * 1000  // アサーション10秒
  },

  use: {
    // ベースURL
    baseURL: 'http://localhost:3000',

    // トレース、スクリーンショット、ビデオ
    trace: 'on-first-retry',
    screenshot: 'only-on-failure',
    video: 'retain-on-failure',

    // ナビゲーションタイムアウト
    navigationTimeout: 30 * 1000,
  },

  // プロジェクト設定（ブラウザ別）
  projects: [
    {
      name: 'chromium',
      use: { ...devices['Desktop Chrome'] },
    },
    {
      name: 'firefox',
      use: { ...devices['Desktop Firefox'] },
    },
    {
      name: 'webkit',
      use: { ...devices['Desktop Safari'] },
    },
  ],

  // テストサーバー起動
  webServer: {
    command: 'npm run dev',
    url: 'http://localhost:3006',
    timeout: 120 * 1000,
    reuseExistingServer: !process.env.CI,
  },
});
```

### グループ別実行スクリプト

**ファイル**: `scripts/run-group-tests.sh`

```bash
#!/bin/bash

# グループ別テスト実行スクリプト

set -e

echo "🚀 テストグループ並列実行開始..."

# 第一波: 状態独立グループ（完全並列）
echo "📦 Wave 1: GROUP A + E + F（完全並列）"
npx playwright test tests/e2e/group-a-public/ \
  tests/e2e/group-e-redirects/ \
  tests/e2e/group-f-database/ \
  --workers=4 \
  --reporter=html

# 第二波: 認証グループ（一部並列）
echo "🔐 Wave 2: GROUP B（認証）"
npx playwright test tests/e2e/group-b-auth/ \
  --workers=2

# 第三波: 会員ポータル（順次実行）
echo "👤 Wave 3: GROUP C（会員ポータル）"
npx playwright test tests/e2e/group-c-member/ \
  --workers=1

# 第四波: 管理者ポータル（順次実行）
echo "👨‍💼 Wave 4: GROUP D（管理者ポータル）"
npx playwright test tests/e2e/group-d-admin/ \
  --workers=1

echo "✅ 全テスト完了"
```

---

## 実行手順

### 全テスト実行（グループ順）

```bash
# 1. 開発サーバー起動
npm run dev &

# 2. サーバー起動待機
sleep 10

# 3. グループ別実行スクリプト実行
bash scripts/run-group-tests.sh

# 4. 結果確認
open playwright-report/index.html
```

### 特定グループのみ実行

```bash
# 公開ページのみ（完全並列）
npx playwright test tests/e2e/group-a-public/ --workers=4

# リダイレクトのみ（完全並列）
npx playwright test tests/e2e/group-e-redirects/ --workers=4

# 会員ポータルのみ（順次実行）
npx playwright test tests/e2e/group-c-member/ --workers=1

# 管理者ポータルのみ（順次実行）
npx playwright test tests/e2e/group-d-admin/ --workers=1
```

### CI/CDパイプラインでの実行

**ファイル**: `.github/workflows/e2e-tests.yml`

```yaml
name: E2E Tests

on:
  push:
    branches: [main, develop]
  pull_request:
    branches: [main]

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3

      - name: Setup Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '18'

      - name: Install dependencies
        run: npm ci

      - name: Install Playwright
        run: npx playwright install --with-deps

      - name: Run Group A Tests (Parallel)
        run: npx playwright test tests/e2e/group-a-public/ --workers=4

      - name: Run Group E Tests (Parallel)
        run: npx playwright test tests/e2e/group-e-redirects/ --workers=4

      - name: Run Group F Tests (Parallel)
        run: npx playwright test tests/e2e/group-f-database/ --workers=4

      - name: Run Group B Tests (Auth)
        run: npx playwright test tests/e2e/group-b-auth/ --workers=2

      - name: Run Group C Tests (Member)
        run: npx playwright test tests/e2e/group-c-member/ --workers=1

      - name: Run Group D Tests (Admin)
        run: npx playwright test tests/e2e/group-d-admin/ --workers=1

      - name: Upload test results
        if: always()
        uses: actions/upload-artifact@v3
        with:
          name: playwright-report
          path: playwright-report/
```

---

## 付録

### テストファイル構成

```
tests/e2e/
├── group-a-public/              # GROUP A: 公開ページ（37テスト）
│   ├── 01-home.spec.ts
│   ├── 02-catalog.spec.ts
│   ├── 03-quote-tools.spec.ts
│   ├── 04-contact.spec.ts
│   └── 05-other.spec.ts
│
├── group-b-auth/                # GROUP B: 認証ページ（12テスト）
│   ├── 01-signin.spec.ts
│   ├── 02-register.spec.ts
│   ├── 03-password-reset.spec.ts
│   └── 04-after-auth.spec.ts
│
├── group-c-member/              # GROUP C: 会員ポータル（26テスト）
│   ├── 01-dashboard.spec.ts
│   ├── 02-orders.spec.ts
│   ├── 03-quotations.spec.ts
│   ├── 04-profile.spec.ts
│   └── 05-other.spec.ts
│
├── group-d-admin/               # GROUP D: 管理者ポータル（18テスト）
│   ├── 01-admin-dashboard.spec.ts
│   ├── 02-orders.spec.ts
│   ├── 03-quotations.spec.ts
│   ├── 04-production.spec.ts
│   └── 05-other.spec.ts
│
├── group-e-redirects/           # GROUP E: リダイレクト（9テスト）
│   ├── 01-portal-redirects.spec.ts
│   ├── 02-b2b-redirects.spec.ts
│   └── 03-other-redirects.spec.ts
│
└── group-f-database/            # GROUP F: DB接続（5テスト）
    ├── 01-correct-connection.spec.ts
    └── 02-incorrect-connection.spec.ts
```

### グループ別テスト一覧

| グループ | サブグループ | テストID | テスト名 | 並列/順次 |
|---------|-------------|----------|----------|----------|
| **A** | A-1 | TC-PUBLIC-001 | トップページ | 並列 |
| **A** | A-1 | TC-PUBLIC-002 | 会社概要 | 並列 |
| **A** | A-2 | TC-PUBLIC-003 | カタログ | 並列 |
| **A** | A-2 | TC-PUBLIC-004 | 製品詳細 | 並列 |
| **B** | B-1 | TC-AUTH-001 | ログインフォーム | 並列 |
| **B** | B-1 | TC-AUTH-002 | バリデーション | 並列 |
| **B** | B-3 | TC-AUTH-003 | ログアウト | 順次 |
| **C** | C-1 | TC-MEMBER-001 | ダッシュボード | 順次 |
| **C** | C-2 | TC-MEMBER-002 | 注文一覧 | 順次 |
| **D** | D-1 | TC-ADMIN-001 | 管理者ダッシュボード | 順次 |
| **E** | E-1 | TC-REDIRECT-001 | Portal → Admin | 並列 |
| **F** | F-1 | TC-DB-001 | 正常接続確認 | 並列 |

---

**文書バージョン**: 1.0
**作成日**: 2026-01-15
**最終修正**: 2026-01-15
