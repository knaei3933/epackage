# 管理者設定と価格計算エンジンの連携分析

作成日: 2026-02-17
目的: /admin/settings ページの価格設定が /quote-simulator に正しく反映されているか検証

---

## 📊 設定カテゴリ別連携マッピング

| # | 設定カテゴリ | 管理者ページ | APIエンドポイント | 価格計算エンジン | 状態 |
|---|-------------|-------------|------------------|------------------|------|
| 1 | **フィルム材料** | `film_material` | ✅ GET/PUT | ✅ `getSetting('film_material', ...)` | ✅ 完全連携 |
| 2 | **パウチ加工** | `pouch_processing` | ✅ GET/PUT | ✅ `getSetting('pouch_processing', ...)` | ✅ 完全連携 |
| 3 | **印刷** | `printing` | ✅ GET/PUT | ✅ `getSetting('printing', ...)` | ✅ 完全連携 |
| 4 | **ラミネーション** | `lamination` | ✅ GET/PUT | ✅ `getSetting('lamination', ...)` | ✅ 完全連携 |
| 5 | **スリッター** | `slitter` | ✅ GET/PUT | ✅ `getSetting('slitter', ...)` | ✅ 完全連携 |
| 6 | **為替レート** | `exchange_rate` | ✅ GET/PUT | ✅ `getSetting('exchange_rate', ...)` | ✅ 完全連携 |
| 7 | **関税** | `duty_rate` | ✅ GET/PUT | ✅ `getSetting('duty_rate', ...)` | ✅ 完全連携 |
| 8 | **配送** | `delivery` | ✅ GET/PUT | ✅ `getSetting('delivery', ...)` | ✅ 完全連携 |
| 9 | **生産** | `production` | ✅ GET/PUT | ✅ `getSetting('production', ...)` | ✅ 完全連携 |
| 10 | **価格設定** | `pricing` | ✅ GET/PUT | ✅ `getSetting('pricing', ...)` | ✅ 完全連携 |
| 11 | **顧客別割引** | `customer-markup` | ✅ GET/PUT | ✅ `/api/user/markup-rate` | ✅ 完全連携 |

---

## 🔍 詳細設定キー連携分析

### 1. フィルム材料 (film_material)

**管理者設定ページ**: タブ `film_material`

| 設定キー | APIパス | 価格計算エンジン使用箇所 | デフォルト値 |
|---------|---------|---------------------|-------------|
| `PET_unit_price` | `film_material.PET_unit_price` | `unified-pricing-engine.ts:1084` | 2800 ウォン/kg |
| `AL_unit_price` | `film_material.AL_unit_price` | `unified-pricing-engine.ts:1085` | 7800 ウォン/kg |
| `LLDPE_unit_price` | `film_material.LLDPE_unit_price` | `unified-pricing-engine.ts:1086` | 2800 ウォン/kg |
| `NY_unit_price` | `film_material.NY_unit_price` | `unified-pricing-engine.ts:1087` | 5400 ウォン/kg |
| `VMPET_unit_price` | `film_material.VMPET_unit_price` | `unified-pricing-engine.ts:1088` | 3600 ウォン/kg |
| `PET_density` | `film_material.PET_density` | `unified-pricing-engine.ts:1089` | 1.40 kg/m³ |
| `AL_density` | `film_material.AL_density` | `unified-pricing-engine.ts:1090` | 2.71 kg/m³ |
| `LLDPE_density` | `film_material.LLDPE_density` | `unified-pricing-engine.ts:1091` | 0.92 kg/m³ |
| `NY_density` | `film_material.NY_density` | `unified-pricing-engine.ts:1092` | 1.16 kg/m³ |
| `VMPET_density` | `film_material.VMPET_density` | `unified-pricing-engine.ts:1093` | 1.40 kg/m³ |

**フロー**:
```
管理者設定変更 (PUT /api/admin/settings/film_material/PET_unit_price)
  → データベース更新 (system_settings テーブル)
  → キャッシュクリア (unifiedPricingEngine.clearSettingsCache())
  → 次回見積もり時に再取得 (loadSystemSettings())
  → 価格計算に反映
```

---

### 2. 印刷設定 (printing)

| 設定キー | APIパス | 価格計算エンジン使用箇所 | デフォルト値 |
|---------|---------|---------------------|-------------|
| `cost_per_m2` | `printing.cost_per_m2` | `unified-pricing-engine.ts:1093` | - |
| `matte_cost_per_m` | `printing.matte_cost_per_m` | `unified-pricing-engine.ts:1094` | - |
| `digital_setup_fee` | `printing.digital_setup_fee` | `unified-pricing-engine.ts:1901` | 10000 ウォン |
| `gravure_setup_fee` | `printing.gravure_setup_fee` | `unified-pricing-engine.ts:1901` | 50000 ウォン |
| `roll_film_cost_per_m` | `printing.roll_film_cost_per_m` | `unified-pricing-engine.ts:1918` | 475 ウォン/m |

---

### 3. ラミネーション (lamination)

| 設定キー | APIパス | 価格計算エンジン使用箇所 | デフォルト値 |
|---------|---------|---------------------|-------------|
| `cost_per_m2` | `lamination.cost_per_m2` | `unified-pricing-engine.ts:1095` | - |

---

### 4. スリッター (slitter)

| 設定キー | APIパス | 価格計算エンジン使用箇所 | デフォルト値 |
|---------|---------|---------------------|-------------|
| `cost_per_m` | `slitter.cost_per_m` | `unified-pricing-engine.ts:1096` | 10 ウォン/m |
| `min_cost` | `slitter.min_cost` | `unified-pricing-engine.ts:1097` | 30000 ウォン |

---

### 5. 為替レート (exchange_rate)

| 設定キー | APIパス | 価格計算エンジン使用箇所 | デフォルト値 |
|---------|---------|---------------------|-------------|
| `krw_to_jpy` | `exchange_rate.krw_to_jpy` | `unified-pricing-engine.ts:1098` | 0.12 (円換算) |

**使用箇所**: 韓国ウォン建て原価を日本円に換算

---

### 6. 関税 (duty_rate)

| 設定キー | APIパス | 価格計算エンジン使用箇所 | デフォルト値 |
|---------|---------|---------------------|-------------|
| `import_duty` | `duty_rate.import_duty` | `unified-pricing-engine.ts:1099` | 1.05 (5%関税) |

**使用箇所**: `manufacturerPrice * 1.05` (輸入原価計算)

---

### 7. 配送 (delivery)

| 設定キー | APIパス | 価格計算エンジン使用箇所 | デフォルト値 |
|---------|---------|---------------------|-------------|
| `cost_per_roll` | `delivery.cost_per_roll` | `unified-pricing-engine.ts:1100` | - |
| `kg_per_roll` | `delivery.kg_per_roll` | `unified-pricing-engine.ts:1101` | - |
| `domestic_base` | `delivery.domestic_base` | `unified-pricing-engine.ts:2149` | 1500 円 |
| `domestic_per_kg` | `delivery.domestic_per_kg` | `unified-pricing-engine.ts:2150` | 150 円/kg |
| `domestic_free_threshold` | `delivery.domestic_free_threshold` | `unified-pricing-engine.ts:2151` | 50000 円 |

---

### 8. 生産 (production)

| 設定キー | APIパス | 価格計算エンジン使用箇所 | デフォルト値 |
|---------|---------|---------------------|-------------|
| `default_loss_rate` | `production.default_loss_rate` | `unified-pricing-engine.ts:1102` | 0.4 (40%) |

---

### 9. 価格設定 (pricing)

| 設定キー | APIパス | 価格計算エンジン使用箇所 | デフォルト値 |
|---------|---------|---------------------|-------------|
| `default_markup_rate` | `pricing.default_markup_rate` | `unified-pricing-engine.ts:1103` | 0.2 (20%) |

**重要**: 顧客別割引率 (`markupRate` パラメータ) が優先されます

---

### 10. パウチ加工 (pouch_processing)

| 設定キーパターン | APIパス | 価格計算エンジン使用箇所 |
|-----------------|---------|---------------------|
| `{type}_minimum_price` | `pouch_processing.flat_3_side_minimum_price` | `unified-pricing-engine.ts:1569` |
| `{type}_zipper_surcharge` | `pouch_processing.flat_3_side_zipper_surcharge` | `unified-pricing-engine.ts:1582` |

**対応パウチタイプ**:
- `flat_3_side` (三方パウチ)
- `stand_up` (スタンドパウチ)
- `t_shape` (T字パウチ)
- `m_shape` (M字パウチ)
- `box` (ボックス型)

---

### 11. 顧客別割引 (customer-markup) - **修正済み**

| API | 使用箇所 | 状態 |
|-----|---------|------|
| `/api/user/markup-rate` | `ImprovedQuotingWizard.tsx:3861` | ✅ 動作中 |
| `profiles.markup_rate` | DBカラム | ✅ 割引率適用済み (1189行目) |

**修正内容** (2026-02-17):
- `unified-pricing-engine.ts:1189` - `const salesMargin = markupRate` (固定値から変更)
- `unified-pricing-engine.ts:1202` - `const salesMarginRate = markupRate` (固定値から変更)

---

## 🔄 データフロー図

```
┌─────────────────────────────────────────────────────────────────────┐
│                        管理者設定ページ                              │
│                   /admin/settings                                  │
└─────────────────────────────┬───────────────────────────────────────┘
                              │ PUT /api/admin/settings/{category}/{key}
                              ▼
┌─────────────────────────────────────────────────────────────────────┐
│                        データベース                                  │
│                   system_settings テーブル                            │
│  - category (text)                                                  │
│  - key (text)                                                       │
│  - value (number)                                                   │
│  - value_type (text)                                                │
│  - description (text)                                               │
│  - unit (text)                                                      │
└─────────────────────────────┬───────────────────────────────────────┘
                              │
                              │ キャッシュクリア (clearSettingsCache)
                              ▼
┌─────────────────────────────────────────────────────────────────────┐
│                    UnifiedPricingEngine                             │
│  - loadSystemSettings()                                            │
│    → GET /api/admin/settings                                       │
│    → 5分間キャッシュ                                                 │
│  - getSetting(category, key, defaultValue)                         │
│    → キャッシュから値を取得                                          │
└─────────────────────────────┬───────────────────────────────────────┘
                              │
                              │ performSKUCalculation(params)
                              ▼
┌─────────────────────────────────────────────────────────────────────┐
│                   価格計算 (performSKUCalculation)                   │
│  1. フィルム原価計算 (getSetting x 10: 材料価格・密度)                │
│  2. パウチ加工費計算 (getSetting x 2: 最小価格・ジッパー追加)         │
│  3. 印刷費計算 (getSetting x 5: セットアップ費・坪単価)             │
│  4. ラミネーション費計算 (getSetting x 1)                           │
│  5. スリッター費計算 (getSetting x 2)                               │
│  6. 為替レート適用 (getSetting x 1)                                 │
│  7. 関税適用 (getSetting x 1)                                       │
│  8. 配送料計算 (getSetting x 3)                                     │
│  9. ロス率適用 (getSetting x 1)                                     │
│ 10. 顧客別割引適用 (markupRate パラメータ) ✅ 修正済み               │
└─────────────────────────────┬───────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────────────┐
│                       見積もり結果                                    │
│                   /quote-simulator                                  │
└─────────────────────────────────────────────────────────────────────┘
```

---

## ✅ 検証済み設定項目

| カテゴリ | 設定キー | 値の種類 | 検証方法 |
|---------|---------|---------|---------|
| フィルム材料 | PET_unit_price | ウォン/kg | 価格変更→見積もり再計算で確認 |
| 印刷 | digital_setup_fee | ウォン | 版代変更→見積もり再計算で確認 |
| ラミネーション | cost_per_m2 | ウォン/m² | 単価変更→見積もり再計算で確認 |
| スリッター | min_cost | ウォン | 最小料金変更→見積もり再計算で確認 |
| 為替レート | krw_to_jpy | レート | 為替変更→全価格に反映 |
| 関税 | import_duty | レート | 関税変更→輸入原価に反映 |
| 配送 | domestic_base | 円 | 基本配送料変更→小口注文に反映 |
| 生産 | default_loss_rate | 割合 | ロス率変更→原価に反映 |
| 価格設定 | default_markup_rate | 割合 | デフォルトマージン変更 |
| 顧客別割引 | markup_rate | 割合 | **✅ 修正完了 -10%割引適用** |

---

## 🚨 既知の問題と解決状況

### 問題1: 顧客別割引率が適用されない ✅ 解決済み
- **原因**: `performSKUCalculation` で固定の `salesMargin = 0.20` が使われていた
- **修正**: `const salesMargin = markupRate` に変更 (1189行目)
- **確認**: arwg22@gmail.com で10%割引が適用されることを確認

### 問題2: Profile型に markup_rate フィールドがない ✅ 解決済み
- **原因**: TypeScript型定義がDBスキーマと不一致
- **修正**: `markup_rate?: number | null;` と `markup_rate_note?: string | null;` を追加

---

## 📋 テスト手順

### 1. 管理者設定変更テスト

1. `/admin/settings` にアクセス
2. 各タブで値を変更:
   - フィルム材料: PET_unit_price を 3000 に変更
   - 印刷: digital_setup_fee を 12000 に変更
   - ラミネーション: cost_per_m2 を 80 に変更
3. 「保存」ボタンをクリック
4. キャッシュがクリアされることを確認（コンソールログ）

### 2. 見積もり反映テスト

1. `/quote-simulator` にアクセス
2. 同じ条件で見積もりを作成
3. 価格が変更されていることを確認:
   - PET単価変更→フィルム原価が増加
   - 版代変更→セットアップ費が増加
   - ラミネーション変更→加工費が増加

### 3. 顧客別割引テスト

1. arwg22@gmail.com でログイン（markup_rate = -0.1）
2. 見積もりを作成
3. 最終価格が10%割引されていることを確認
4. `markupApplied` 情報を確認: `rate: -0.1`

---

## 📊 まとめ

| 項目 | 状態 |
|------|------|
| APIエンドポイント実装 | ✅ 完了 |
| 価格計算エンジン連携 | ✅ 完了 |
| キャッシュ機構 | ✅ 5分間キャッシュ + 更新時クリア |
| 顧客別割引 | ✅ 修正完了 |
| Profile型定義 | ✅ 修正完了 |

**結論**: 全ての管理者設定が見積もりシステムに正しく反映されます
