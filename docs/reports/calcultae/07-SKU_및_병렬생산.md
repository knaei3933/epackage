# 07. SKU概念と並列生産戦略

> **対象**: SKU（Stock Keeping Unit）別原価計算と並列生産最適化、段階的提案戦略
> **更新日**: 2026-01-29（v2.2: 固定総数量モード、SKU追加料金、自動割引適用）

---

> **Navigation**: [← Prev: 06. マージン計算](./06-마진_및_최종가격.md) | [🏠 Home](./00-README.md) | [Next: 08. シナリオ別計算 (KR) →](./08-비즈니스_시나리오_모음.md)

---

## 1. SKUとは

### 1-1. 定義

**SKU（Stock Keeping Unit）** = 在庫管理単位

このシステムにおけるSKUは、**異なるデザイン/柄を持つパウチ**を表します。

| SKU数 | 説明 | 例 |
|-------|------|------|
| **1 SKU** | 1つのデザイン | チョコ味パウチのみ |
| **2 SKU** | 2つの異なるデザイン | チョコ味 + ストロベリー味 |
| **3 SKU** | 3つの異なるデザイン | チョコ + ストロベリー + 抹茶味 |

### 1-2. SKUの使用場面

- **フレーバーバリエーション**: 同じサイズで異なる味/デザイン
- **パッケージデザイン**: 同じ製品で異なるラベルデザイン
- **言語バージョン**: 同じ製品で日本語版・英語版
- **プロモーション**: 通常版 + 限定版デザイン

### 1-3. SKU追加料金

**重要**: 複数SKUの生産には追加コストが発生します

| SKU数 | 追加料金 | 計算式 |
|-------|---------|--------|
| **1 SKU** | ¥0 | - |
| **2 SKUs** | +¥10,000 | (2 - 1) × ¥10,000 |
| **3 SKUs** | +¥20,000 | (3 - 1) × ¥10,000 |
| **4 SKUs** | +¥30,000 | (4 - 1) × ¥10,000 |

> **理由**: 複数SKUの管理、印刷版の作成、品質管理などに追加コストが発生するため

### 1-4. SKUグループの核心ルール

**重要**: **同じピッチ（幅）のSKUグループでロス400m固定**

| SKU数 | 各SKU数 | 理論メートル | 確保長 | ロス | 総フィルム |
|-------|--------|-------------|--------|------|-----------|
| 1 SKU | 500個 | 65m | 65m | 400m | **465m** |
| 2 SKU | 500個×2 | 65m×2 | 130m | 400m | **530m** |
| 3 SKU | 500個×3 | 65m×3 | 195m | 400m | **595m** |

> **重要**: 追加SKUごとに理論メートルが増えるが、ロスは変わらない（400mは何SKUでもOK）

---

## 2. 並列生産（2列生産）戦略

### 2-1. 並列生産とは

**並列生産**は、1つの原反から複数のパウチを同時に生産する手法です。

> **対象製品**: スタンドパウチ、平袋、スパウトパウチ（2列まで）
> **ロールフィルム**: 最大7列まで並列生産可能

### 2-2. 並列生産の条件

```
条件:
- 理論メートル >= 1,000m（ロス除く）
- 2列フィルム総幅 <= 740mm

フィルム総幅計算（スタンドパウチ）:
- 1列: (H × 2) + G + 35
- 2列: (H × 4) + (G × 2) + 40

例: 130×130×30mm
- 1列: (130 × 2) + 30 + 35 = 325mm → 590mm原反使用
- 2列: (130 × 4) + (30 × 2) + 40 = 600mm → 760mm原反使用 ✅ OK
```

### 2-3. 2列生産の経済性

#### フィルム幅効率の改善

2列生産の本質は**フィルム幅の効率的利用**です。

| 項目 | 1列生産 | 2列生産 | 効率改善 |
|------|---------|---------|---------|
| **フィルム幅** | 325mm | 600mm | - |
| **使用原反** | 590mm | 760mm | - |
| **無駄** | 265mm（45%） | 160mm（21%） | **24%改善** |
| **原価** | 100% | **70%** | **30%削減** 💡 |

> **重要**: ロス400mは固定で、2列生産の経済性はフィルム幅効率から生じます
>
> ❌ 誤解: 「ロス400mを活用して2列生産」
> ✅ 正解: 「760mm原反をより効率的に使用して2列生産」

#### 原価削減の内訳（30%削減）

| 項目 | 1列生産 | 2列生産（同数） | 削減率 |
|------|---------|---------------|--------|
| **原材料費** | 100% | **70%** | **30%削減** |
| 印刷費 | 100% | 140%（×2） | 40%増加 |
| ラミネート費 | 100% | 140%（×2） | 40%増加 |
| スリッター費 | 100% | 140%（×2） | 40%増加 |
| パウチ加工費 | 100% | 100% | 同じ |
| **合計原価** | 100% | **約70%** | **30%削減** |

### 2-4. 2列生産の2つのパターン

#### パターンA: 同じ数量で2列生産（最推奨）💎

**概念**: 同じ数量を2列で生産して原価を30%削減

| 項目 | 1列生産 | 2列生産（同数） | 効果 |
|------|---------|---------------|------|
| 生産数 | 7,690個 | 7,690個 | 同じ |
| **原価** | 100% | **70%** | **30%削減** 💡 |
| 販売者マージン | 20% | **35%** | **+15%** 💰 |
| 顧客価格 | 315,290円 | **269,150円** | **15% OFF** |
| 単価 | 41円/個 | **35円/個** | **6円安** |

**Win-Win構造**:
- 顧客: 15%割引で同じ数量を安く購入
- 販売者: マージン20% → 35%（+15%上乗せ）

#### パターンB: 同じフィルムで2倍生産

**概念**: 同じフィルム使用量で2倍の個数を生産

| 項目 | 1列生産 | 2列生産（倍量） | 効果 |
|------|---------|---------------|------|
| フィルム | 1,440m | 1,440m | 同じ |
| 生産数 | 11,074個 | **22,148個** | **2倍** |
| **原価** | 100% | **70%** | **30%削減** 💡 |
| 販売者マージン | 20% | **40%** | **+20%** 💰 |
| 顧客価格 | 387,590円 | 531,552円 | **30% OFF** |
| 単価 | 35円/個 | **24円/個** | **11円安** |

**Win-Win構造**:
- 顧客: 30%割引で大量購入（単価35円 → 24円）
- 販売者: マージン20% → 40%（+20%上乗せ）、売上44%増

### 2-5. 自動割引適用（新機能）

**並列生産可能数量の場合、自動的に割引価格を表示**

| 条件 | 表示価格 | 割引率 |
|------|---------|--------|
| 総数量 >= 1,000個かつ2列生産可能 | **15% OFF価格** | 自動適用 💡 |

**動作フロー**:

```
ユーザー入力: 10,000個
    ↓
システム判定: 2列生産可能（1,000個以上、フィルム幅条件OK）
    ↓
基本価格: 39円/個 × 10,000 = 390,000円
    ↓
自動割引適用: 35円/個 × 10,000 = 350,000円（15% OFF）
    ↓
表示: 「💎 2列生産適用 (15% OFF)」バッジ
    ↓
ユーザー操作可能:
  - 「1列生産に戻す」ボタンで元価格に戻す
  - SKU追加/削除で総数量を再配分
  - 個別SKU数量を自由調整（総数量維持）
```

> **重要**: ユーザーが明示的に適用ボタンを押す必要がなく、最適価格が自動的に表示される

### 2-6. 固定総数量モード（新機能）

**割引適用後の総数量を固定し、SKU間で自由に数量配分**

#### 機能概要

| 項目 | 説明 |
|------|------|
| **総数量固定** | 割引適用時の総数量（例：10,000個）を固定 |
| **SKU数量調整** | 各SKUは100単位、最小500個で自由調整可能 |
| **総数量バリデーション** | 総数量が固定数量と一致しない場合はエラー表示 |
| **SKU追加/削除** | 総数量を均等分割して自動配分 |

#### 使用例

**シナリオ**: 10,000個で15%割引適用後、3 SKUに配分

```
初期状態:
  SKU1: 10,000個（1 SKU、総数10,000個、15%割引適用）

SKU追加:
  SKU1: 3,300個（100単位で丸め）
  SKU2: 3,300個
  SKU3: 3,300個
  総数: 9,900個（100単位制限により-100個）
  割引: 維持 ✅

手動調整:
  SKU1: 5,000個
  SKU2: 3,000個
  SKU3: 2,000個
  総数: 10,000個
  割引: 維持 ✅

エラー例:
  SKU1: 6,000個
  SKU2: 3,000個
  SKU3: 2,000個
  総数: 11,000個
  エラー: 「総数量が+1,000個です。総数量10,000個を維持してください」
  次ページへ: 進めない ❌
```

#### UI表示

**固定総数量モード時の通知バナー**:
```
💎 2列生産割引適用中 - 総数量10,000個

各SKUの数量は100単位で自由に調整可能です（最小500個）。
総数量が10,000個になるように配分してください。

現在の総数量: 10,000個 ✓ 割引維持
```

**バリデーションエラー時**:
```
⚠️ 総数量が+1,000個です。総数量10,000個を維持してください

総数量を10,000個に合わせると割引が維持されます
```

### 2-7. SKU追加/削除時の挙動

| 操作 | 挙動 | 割引 |
|------|------|------|
| **SKU追加（固定数量モード）** | 総数量を新しいSKU数で均等分割（100単位） | 維持 ✅ |
| **SKU削除（固定数量モード）** | 総数量を新しいSKU数で均等分割（100単位） | 維持 ✅ |
| **数量パターン適用** | 総数量が変わるため割引解除 | 解除 ❌ |
| **手動数量調整** | 総数量が変わる場合、エラー表示 | 次ページで検証 |

**均等分割の計算**:
```
総数量: 10,000個
新SKU数: 3

計算:
  10,000 ÷ 3 = 3,333.33...
  100単位で丸め: 3,300個
  最小数量チェック: 3,300 >= 500 ✅

結果:
  SKU1: 3,300個
  SKU2: 3,300個
  SKU3: 3,300個
  総数: 9,900個（100単位制限）
```

> **重要**: 100単位で丸める関係上、総数量が若干減る場合がありますが、割引は維持されます

---

## 3. 段階的提案戦略

### 3-1. 戦略的概念

顧客に「もっと買いやすい」と思わせる段階的提案フロー：

```
ある顧客が7,000個を検討中
システムからの提案：
┌─────────────────────────────────────────┐
│ 現在の選択肢：7,000個                     │
│  - 42円/個                              │
│  - 294,000円                            │
└─────────────────────────────────────────┘
              ↓
┌─────────────────────────────────────────┐
│ 💎 経済的数量：7,690個                   │
│  - 41円/個                              │
│  - 315,290円                            │
│       (+690個で+21,290円、単価少し安くなる) │
└─────────────────────────────────────────┘
              ↓
┌─────────────────────────────────────────┐
│ 💎 経済的数量（2列）：7,690個            │
│  - 35円/個                              │
│  - 269,150円                            │
│       (同じ数量で46,140円お得！15%割引)  │
└─────────────────────────────────────────┘
              ↓
┌─────────────────────────────────────────┐
│ 💎 2列生産（倍量）：22,148個             │
│  - 24円/個                              │
│  - 531,552円                            │
│       (同じ1,440mフィルムで！価格30%割引!)│
└─────────────────────────────────────────┘
              ↓
┌─────────────────────────────────────────┐
│ 💎 SKU追加なら：2 SKUで各7,000個        │
│  - 単価24円/個                           │
└─────────────────────────────────────────┘

顧客の思考プラン：
├─ 「あ、690個ならほとんど価格変わらないね」
│   └→ 690個お得（+21,290円で+690個）
│
├─ 「待って、2列生産なら単価が35円に！？」
│   └─ 同じ数量で46,140円お得！
│
└─ 「じっあSKU増やして15,000個注文しようかな？」
    └─ もっと買うほど安くなることが面白い
```

### 3-2. 段階提案の意味

| 段階 | 提案内容 | 顧客の心理 | 効果 |
|------|----------|-----------|------|
| **1段階目** | 経済的数量提案 | ほぼ同価格でちょっと得 | 少量ブースト |
| **2段階目** | 2列生産提案 | 単価が30%以上安くなる！ | 大幅ブースト |
| **3段階目** | SKU追加提案 | もっと買うほど安くなる | LTV最大化 |

### 3-3. 提案例（スタンドパウチ 130×130×30mm）

#### ケース1: 7,000個リクエスト

| 提案 | 数量 | 単価 | 総額 | 差額 | メリット |
|------|------|------|------|------|----------|
| 📋 現在の選択肢 | 7,000個 | 42円/個 | 294,000円 | - | 現在のリクエスト |
| 💎 経済的数量（1列） | 7,690個 | **41円/個** | 315,290円 | +21,290円 | +690個で単価1円安 |
| 💎 経済的数量（2列） | 7,690個 | **35円/個** | 269,150円 | -24,850円 | **同じ数量で15%割引！** ⭐⭐⭐ |
| 💎 2列生産（倍量） | 22,148個 | **24円/個** | 531,552円 | +237,552円 | 同じフィルムで！30%割引 |

#### ケース2: 10,000個リクエスト（自動割引適用）

| 提案 | 数量 | 単価 | 総額 | 差額 | メリット |
|------|------|------|------|------|----------|
| 📋 現在の選択肢 | 10,000個 | 39円/個 | 390,000円 | - | 現在のリクエスト |
| 💎 **自動適用: 2列生産** | 10,000個 | **35円/個** | **350,000円** | **-40,000円** | **15% OFF自動適用！** ⭐⭐⭐ |
| 💎 経済的数量 | 13,073個 | **38円/個** | 496,774円 | +106,774円 | +3,073個で単価同じ |
| 💎 2列生産（倍量） | 26,146個 | **29円/個** | 758,234円 | +408,234円 | 同じフィルムで！24%割引 |

> **重要**: 10,000個は2列生産可能なため、自動的に15% OFF価格が表示されます

#### ケース3: 15,000個リクエスト（3 SKU配分）

| 提案 | 数量 | 単価 | 総額 | SKU配分 |
|------|------|------|------|---------|
| 📋 **自動適用: 2列生産** | 15,000個（1 SKU） | **33円/個** | **495,000円** | SKU1: 15,000個 |
| 💎 SKU追加（総数維持） | 15,000個（3 SKU） | **34円/個** | **510,000円** | SKU1-3: 各5,000個 |
| 💎 SKU追加（手動配分） | 15,000個（3 SKU） | **34円/個** | **510,000円** | SKU1: 5,000個<br>SKU2: 5,000個<br>SKU3: 5,000個 |

> **注**: SKU追加料金 +¥20,000が含まれます

---

## 4. SKU別原価計算ルール

### 4-1. 商品タイプ別ルール

> **重要**: ロスは**同じピッチ（パウチ）または同じ幅（ロール）のSKUグループごとに400m固定**

#### パウチ商品

| 項目 | 設定値 |
|------|--------|
| **最小確保量** | **なし** |
| **切り上げ単位** | 1m単位 |
| **ロス** | 同じピッチのSKUグループごとに400m固定 |

> **理由**: 1種類（1 SKU）のデザインを印刷する場合でも、100種類（100 SKU）のデザインを印刷する場合でも、同じピッチならセットアップロスは400mで一定。追加SKUに対するロスは発生しません。

#### ロールフィルム商品

| SKU数 | 各SKU最小確保量 | 切り上げ単位 |
|--------|-----------------|-------------|
| **1種類** | 500m | 50m単位 |
| **2種類以上** | 各300m | 50m単位 |

| 項目 | 設定値 |
|------|--------|
| **ロス** | 同じ幅のSKUグループごとに400m固定 |

### 4-2. SKU追加のメリット

**初期クラス**: 1 SKUで7,000個
```
1 SKUだけで生産:
各SKU数量: 7,000個
理論メートル: 910m
総フィルム: 1,310m
生産可能数量: 10,073個
価格: 42円/個
総額: 294,000円（7,000個分）
```

**SKU追加提案**:

```
2 SKUで生産:
SKU 1: チョコ味 7,000個
SKU 2: ストロベリー味 7,000個
理論メートル: 910m × 2 = 1,820m
総フィルム: 1,820m + 400m = 2,220m（ロスは1回だけ！）
生産可能数量: 2,220m × 7.69 = 17,071個/各SKU

実際生産: 8,000個（合計16,000個）
価格: 36円/個（1 SKUより6円安！）
総額: 504,000円（16,000個分）
SKU追加料金: +¥10,000
最終総額: 514,000円
メッセージ: 追加SKUで単価さらに安（2 SKUで36円/個）
→ 2つのデザインで4,000個確保 + ロス400m分は各SKUで共有
```

**3 SKUへの拡張**:

```
3 SKUで生産:
SKU 1: チョコ味 7,000個
SKU 2: ストロベリー味 7,000個
SKU 3: 抹茶味 7,000個
理論メートル: 910m × 3 = 2,730m
総フィルム: 2,730m + 400m = 3,130m（ロスは1回だけ！）
生産可能数量: 3,130m × 7.69 = 24,070個/各SKU

実際生産: 8,000個（合計24,000個）
価格: 34円/個（2 SKUよりさらに安！）
総額: 714,000円（24,000個分）
SKU追加料金: +¥20,000
最終総額: 734,000円
メッセージ: 3 SKUでさらに価格安（36円 → 34円）
→ 3つのデザインで1,000個確保 + ロス400m分は各SKUで共有
```

### 4-3. 確保量計算フロー

#### パウチ商品

```
1. 同じピッチのSKUをグループ化
   ↓
2. 各グループの理論メートル数を計算・合計
   ↓
3. 1m単位で切り上げ（最小確保量なし）
   ↓
4. ロス追加（グループごとに400m固定）
   ↓
5. 総メートル数確定
```

**例**: パウチ 2 SKUで各500個注文（同じピッチ120mm）

```
理論メートル数 = 500個 ÷ (1000 ÷ 120) = 60m

グループ合計:
  - SKU 1: 60m
  - SKU 2: 60m
  - 理論メートル数合計: 120m
  - 確保量: 120m（1m単位、最小制限なし）
  - ロス: 400m（グループごと）
  - 総フィルム: 520m
```

---

## 5. 計算式の基本

### 5-1. 理論メートル計算

```
理論メートル = 数量 ÷ (1,000mm ÷ ピッチ)

例: 7,000個、ピッチ130mm
理論メートル = 7,000 ÷ (1,000 ÷ 130)
              = 7,000 ÷ 7.69
              = 910m
```

### 5-2. 総フィルム量

```
総フィルム量 = 理論メートル + 400m（ロス）

例: 7,000個、ピッチ130mm
総フィルム量 = 910m + 400m = 1,310m
→ 1,310mで生産可能数 = 1,310 × 7.69 = 10,073個
→ 経済的数量 = 10,073個（+3,073個お得）
```

### 5-3. 価格計算の詳細

```
■ 原材料費 = 厚さ(μm) ÷ 1,000 × 幅(m) × メートル数 × 比重 × 価格
■ 印刷費 = **1m** × メートル数 × 475円/m²（常に1mで計算！）
■ ラミネート費 = 幅(m) × メートル数 × 75円/m × (層数 - 1)
■ スリッター費 = MAX(30,000円、メートル数 × 10円）
■ パウチ加工費 = MAX(基本計算、最低単価）
   - スタンド: 1.2円/(個cm × cm) × 数量（最低50,000円）
   - 平袋: 0.4円/(個cm × cm) × 数量（最低100,000円）

基礎原価 = 材料 + 印刷 + ラミネート + スリッター + 加工
製造者価格 = 基礎原価 × 1.4（40%利益マージン）
円貨製造者価格 = 製造者価格 × 0.12（為替レート調整）
消費税 = 円貨製造者価格 × 0.05
運送料 = 127,980円 × 0.12 = 15,358円
小計 = 円貨製造者価格 + 消費税 + 運送料
最終価格 = 小計 × 1.2（20%販売マージン）

SKU追加料金 = (SKU数 - 1) × ¥10,000
総額 = 最終価格 + SKU追加料金
```

---

## 6. 実装ガイドライン

### 6-1. 数量オプション生成

```typescript
interface QuantityOption {
  label: string;
  quantity: number;
  unitPrice: number;
  totalPrice: number;
  highlight?: string;
  reason?: string;
  isDefault?: boolean;
  autoApply?: boolean; // 自動適用フラグ
}

function generateQuantityOptions(
  requestedQuantity: number,
  pouchType: string,
  dimensions: Dimensions,
  skuCount: number = 1
): QuantityOption[] {

  const options: QuantityOption[] = [];
  const pitch = dimensions.width; // ピッチ = pouch横
  const pouchesPerMeter = 1000 / pitch;
  const theoreticalMeters = requestedQuantity / pouchesPerMeter;
  const totalFilm = theoreticalMeters + 400; // ロス追加
  const economicQuantity = Math.floor(totalFilm * pouchesPerMeter);

  // 1. 現在のリクエスト
  const currentPrice = calculatePrice(requestedQuantity, pouchType, dimensions, skuCount);
  options.push({
    label: "選択肢",
    quantity: requestedQuantity,
    unitPrice: Math.round(currentPrice / requestedQuantity),
    totalPrice: Math.round(currentPrice),
    isDefault: true
  });

  // 2. 経済的数量提案
  if (economicQuantity > requestedQuantity && economicQuantity <= requestedQuantity * 1.2) {
    const economicPrice = calculatePrice(economicQuantity, pouchType, dimensions, skuCount);
    const currentUnitPrice = currentPrice / requestedQuantity;
    const economicUnitPrice = economicPrice / economicQuantity;
    const priceDiff = Math.round(currentUnitPrice - economicUnitPrice);

    options.push({
      label: "経済的数量",
      quantity: economicQuantity,
      unitPrice: Math.round(economicUnitPrice),
      totalPrice: Math.round(economicPrice),
      highlight: "💎",
      reason: `+${economicQuantity - requestedQuantity}個${priceDiff > 0 ? `で単価${priceDiff}円安` : ''}`
    });
  }

  // 3. 2列生産提案（同じ数量）- 自動適用
  const filmWidth2Columns = calculateFilmWidth2Columns(pouchType, dimensions);
  if (filmWidth2Columns <= 740 && theoreticalMeters >= 1000) {
    const twoColumnPrice = calculatePrice(requestedQuantity, pouchType, dimensions, skuCount, true);
    const currentUnitPrice = currentPrice / requestedQuantity;
    const twoColumnUnitPrice = twoColumnPrice / requestedQuantity;
    const discountRate = Math.round((1 - twoColumnUnitPrice / currentUnitPrice) * 100);

    if (discountRate >= 15) {
      options.push({
        label: "経済的数量（2列）",
        quantity: requestedQuantity,
        unitPrice: Math.round(twoColumnUnitPrice),
        totalPrice: Math.round(twoColumnPrice),
        highlight: "💎",
        reason: `同じ数量で${discountRate}%割引！`,
        autoApply: true // 自動適用
      });
    }

    // 4. 2列生産提案（倍量）
    const doubleQuantity = requestedQuantity * 2;
    const doublePrice = calculatePrice(doubleQuantity, pouchType, dimensions, skuCount, true);
    const doubleUnitPrice = doublePrice / doubleQuantity;
    const doubleDiscountRate = Math.round((1 - doubleUnitPrice / currentUnitPrice) * 100);

    options.push({
      label: "2列生産（倍量）",
      quantity: doubleQuantity,
      unitPrice: Math.round(doubleUnitPrice),
      totalPrice: Math.round(doublePrice),
      highlight: "💎",
      reason: `同じフィルムで！${doubleDiscountRate}%割引`
    });
  }

  return options;
}
```

### 6-2. 固定総数量モード実装

```typescript
interface FixedTotalQuantityState {
  fixedTotalQuantity: number; // 固定総数量
  twoColumnOptionApplied: 'same' | 'double' | null;
  originalUnitPrice: number; // 元の単価
  discountedUnitPrice: number; // 割引後単価
}

// SKU数量変更時のバリデーション
function validateSKUQuantityChange(
  index: number,
  newValue: number,
  currentState: FixedTotalQuantityState,
  currentQuantities: number[]
): { valid: boolean; error?: string } {

  // 100単位チェック
  if (newValue % 100 !== 0) {
    return {
      valid: false,
      error: `SKU ${index + 1}の数量は100単位で入力してください`
    };
  }

  // 最小数量チェック
  if (newValue < 500) {
    return {
      valid: false,
      error: `SKU ${index + 1}の数量は最小500個です`
    };
  }

  // 総数量チェック
  const newTotalQuantity = currentQuantities.reduce((sum, qty, idx) => {
    return sum + (idx === index ? newValue : qty);
  }, 0);

  if (newTotalQuantity !== currentState.fixedTotalQuantity) {
    const diff = newTotalQuantity - currentState.fixedTotalQuantity;
    return {
      valid: false,
      error: `総数量が${diff > 0 ? '+' : ''}${diff}個です。総数量${currentState.fixedTotalQuantity.toLocaleString()}個を維持してください`
    };
  }

  return { valid: true };
}

// SKU追加時の均等分割
function redistributeQuantityOnSKUAdd(
  fixedTotalQuantity: number,
  newSKUCount: number
): number[] {

  // 100単位で均等分割
  const quantityPerSKU = Math.floor(fixedTotalQuantity / newSKUCount / 100) * 100;

  // 最小数量チェック
  const minQuantityPerSku = 500;
  const adjustedQuantityPerSKU = Math.max(quantityPerSKU, minQuantityPerSku);

  // すべてのSKUに同じ数量を配分
  return Array(newSKUCount).fill(adjustedQuantityPerSKU);
}
```

---

## 7. UI実装例

### 7-1. 固定総数量モードの表示

```tsx
{/* 固定総数量モード時の通知バナー */}
{twoColumnOptionApplied && fixedTotalQuantity && (
  <div className="mb-4 p-4 bg-purple-50 border-2 border-purple-300 rounded-lg">
    <div className="flex items-start gap-2">
      <span className="text-purple-600 text-lg">💎</span>
      <div className="flex-1">
        <p className="text-purple-900 font-medium mb-1">
          2列生産割引適用中 - 総数量{fixedTotalQuantity.toLocaleString()}個
        </p>
        <p className="text-sm text-purple-700 mb-2">
          各SKUの数量は100単位で自由に調整可能です（最小500個）。
          総数量が{fixedTotalQuantity.toLocaleString()}個になるように配分してください。
        </p>
        <div className="text-xs text-purple-600 bg-purple-100 rounded p-2">
          現在の総数量: <strong>{totalQuantity.toLocaleString()}個</strong>
          {totalQuantity === fixedTotalQuantity ? (
            <span className="ml-2 text-green-700">✓ 割引維持</span>
          ) : (
            <span className="ml-2 text-red-700">✗ 総数量が一致しません</span>
          )}
        </div>
      </div>
    </div>
  </div>
)}

{/* 総数量バリデーションエラー */}
{totalQuantityError && (
  <div className="mb-4 p-4 bg-yellow-50 border-2 border-yellow-300 rounded-lg">
    <div className="flex items-start gap-2">
      <span className="text-yellow-600 text-lg">⚠️</span>
      <div className="flex-1">
        <p className="text-yellow-900 font-medium">{totalQuantityError}</p>
        <p className="text-xs text-yellow-700 mt-1">
          総数量を{fixedTotalQuantity?.toLocaleString()}個に合わせると割引が維持されます
        </p>
      </div>
      <button onClick={() => setTotalQuantityError(null)}>
        ✕
      </button>
    </div>
  </div>
)}
```

### 7-2. SKU数量入力（固定総数量モード）

```tsx
<div className="relative">
  <input
    type="number"
    min="500"
    max="1000000"
    step="100"
    value={skuQuantities[index]}
    onChange={(e) => handleSKUQuantityChange(index, e.target.value)}
    className={`
      w-full px-3 py-2 border rounded-lg focus:ring-2
      ${twoColumnOptionApplied && fixedTotalQuantity
        ? 'bg-purple-50 border-purple-300 focus:ring-purple-500'
        : 'border-gray-300 focus:ring-blue-500'
      }
    `}
    placeholder="数量を入力（最小500個、100単位）"
  />
</div>
```

---

## 8. 関連ファイル

| ファイル | 説明 |
|---------|------|
| `src/lib/pouch-cost-calculator.ts` | SKU別原価計算エンジン |
| `src/lib/unified-pricing-engine.ts` | 統合価格計算エンジン（SKU追加料金対応） |
| `src/components/quote/UnifiedSKUQuantityStep.tsx` | 統合SKU数量UI（最大100 SKU、固定総数量モード対応） |
| `src/contexts/QuoteContext.tsx` | SKU状態管理（固定総数量状態対応） |

---

## 9. まとめ

### 9-1. 戦略的要点

| 要点 | 説明 |
|------|------|
| **経済的数量** | 同じフィルムで最大生産数を提案 |
| **2列生産（同数）** | 同じ数量で2列生産、単価15%割引（自動適用） ⭐最推奨 |
| **2列生産（倍量）** | 同じフィルムで2列生産、単価30%割引 |
| **固定総数量モード** | SKU間で自由に数量配分、総数量維持で割引継続 💡新機能 |
| **SKU追加料金** | SKU数に応じて追加料金発生（1 SKU: ¥0、2 SKUs: +¥10,000） |
| **段階的提案** | 少しずつ数量を増やしてもらうフロー |

### 9-2. 顧客へのメッセージ例

```
💎 お得な提案

├─ 7,690個（1列）: 単価41円、総額315,290円
├─ 7,690個（2列）: 単価35円、総額269,150円
│   └── 同じ数量で46,140円お得！15%割引 💎最推奨
└─ 22,148個（2列）: 単価24円、総額531,552円
    └── 同じフィルムで！30%割引

💡 新機能: 固定総数量モード
   割引適用後、各SKUの数量を自由に配分可能！
   例：10,000個（2列）→ SKU1: 5,000個、SKU2: 3,000個、SKU3: 2,000個

結論: 7,690個必要なら2列生産が一番お得！
```

### 9-3. Win-Win構造

**2列生産（同数）の場合**:
- 顧客: 単価15%割引（41円 → 35円）、同じ数量で46,140円お得
- 販売者: 原価30%削減、マージン35%（+15%上乗せ）

**2列生産（倍量）の場合**:
- 顧客: 単価30%割引（35円 → 24円）、大量購入で大幅安
- 販売者: 原価30%削減、マージン40%（+20%上乗せ）、売上44%増

**固定総数量モード**:
- 顧客: SKU間で自由に数量配分、総数量維持で割引継続
- 販売者: 柔軟な対応で顧客満足度向上、注文単価維持

---

## 10. 次のステップ

SKU概念と並列生産戦略を理解したら、次のドキュメントで具体的な計算方法を確認してください：

- [`08-비즈니스_시나리오_모음.md`](./08-비즈니스_시나리오_모음.md) - 다양한 シナリオ計算例시 (Korean)

> [Next: 08. シナリオ別計算 (KR) →](./08-비즈니스_시나리오_모음.md)

---

**更新日**: 2026-01-29
**バージョン**: 2.2（固定総数量モード、SKU追加料金、自動割引適用）
